# Training-only deps (used by train.py / evaluate.py and the build stage).
# Kept out of the runtime image.
-r requirements.txt
pandas==2.2.*
scikit-learn==1.4.*
accelerate==0.27.*  # required by HF Trainer (>=0.21) for the PyTorch training loop
