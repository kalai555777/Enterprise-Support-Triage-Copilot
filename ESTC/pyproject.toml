[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
# Skip virtual-environment files
exclude = ["Lib", "Scripts", "Include", "pyvenv.cfg", ".claude"]

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.mypy]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "shared.schemas.*"
strict = true

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"